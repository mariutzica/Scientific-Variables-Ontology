Instructions to run the ontology endpoint on your own machine:

1. Download Apache Jena Fuseki: https://jena.apache.org/documentation/fuseki2/
2. In the folder tree there are two paths for startup files.
  (a) Place the ds.ttl in run/configuration.
  (b) Place geo-rules.rules in run/databases.
3. Run the server with one of the following commands:
  (a) ./fuseki-server
  (b) ./fuseki start (as a system service)
